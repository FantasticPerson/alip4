02变量的意义，堆栈的世界，语言执行规则

1：宿主环境宿主环境对象
    浏览器=》宿主环境 javascript 不提供输出跟输入 宿主环境对象 window
        1：BOM window对象
        2：宿主环境对象 也是window对象 全局作用域 window对象
    全局变量 window对象的属性
    全局函数 window对象的方法
2：内存的概念
    常量 常量池   值类型的值
    变量 栈
    对象 堆       引用类型的值

    js加载顺序
    先定义 var变量 function函数 从上往下
    后执行 除定义代码其余都是执行代码
3：变量的意义
    用来存储地址

    先开辟变量的内存空间
    再去拿对象的内存地址
4：语言执行规则